# PRDs

This folder contains product requirement docs and feature planning notes.

Use PRDs for:

- experience design before implementation
- brainstorming tradeoffs
- agreeing on behavior before coding

PRDs are planning documents, not the canonical record of shipped behavior.

After implementation:

- update the relevant canonical docs in `docs/FEATURES/`
- update `docs/CHANGELOG.md` if behavior changed

